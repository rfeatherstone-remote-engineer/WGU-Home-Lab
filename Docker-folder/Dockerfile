# Use a lightweight, high-performance web server image
FROM nginx:alpine

# Copy your custom HTML page into the web server's default public folder
COPY index.html /usr/share/nginx/html/

# Tell Docker to open up port 80 to web traffic
EXPOSE 80
