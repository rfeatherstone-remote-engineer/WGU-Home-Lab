apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitops-chaos-nginx
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gitops-chaos-nginx
  template:
    metadata:
      labels:
        app: gitops-chaos-nginx
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80
